word_per_page = 0

pages = int(input("Number of pages:"))

word_per_page == int(input("Number of words per page:"))
print(f"Pages = {pages} Words per page = {word_per_page}") 
total_words = pages * word_per_page
print(total_words)

# '==" is used to check if the values are equal and return a boolean but since we want to assign the value, '=' must be used."

word_per_page = int(input("Number of words per page:"))
print(f"Pages = {pages} Words per page = {word_per_page}") 
total_words = pages * word_per_page
print(total_words)